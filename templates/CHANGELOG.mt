# Changelog — 变更记录

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。
Format follows Keep a Changelog. Semantic versioning is per-module.

## [未发布 / Unreleased]

### Added（新增）
- 新增 `<feature>`。

### Changed（变更 / 文档 / 合规）
- 新增/更新 `<doc/compliance change>`。

### Security（安全）
- 补脱敏 `<x>`；`<module>.<func>` 增加 `<validation>`。

### Removed（移除）
- 清理 `<x>`。

## [0.1.0] - YYYY-MM-DD（首发 / Initial release）
- 脱敏与合规就绪；公开层初始化。